(()=>{document.querySelector('.sim-signature').textContent='Simuladores interativos utilizados nas disciplinas de Fisiologia Humana do Prof. Dr. Mário César Nascimento ©';
const balance=document.getElementById('valBalanco');const syncColor=()=>{const color=parseFloat(balance.textContent)<0?'var(--rust)':'var(--teal)';balance.closest('.readout').style.setProperty('--card-accent',color);document.querySelectorAll('.summary-line:has([id^=graph])').forEach(e=>{e.style.setProperty('--card-accent',color);e.querySelector('b').style.color=color});document.querySelector('.chart-wrap .legend i').style.background=color};new MutationObserver(syncColor).observe(balance,{attributes:true,attributeFilter:['style'],childList:true});syncColor();
const d=document.createElement('dialog');d.id='wolff-dialog';d.setAttribute('aria-label','Estrutura óssea ampliada');d.innerHTML='<button type="button" autofocus>Fechar</button>';document.body.append(d);let anchor,svg,opener;d.querySelector('button').onclick=()=>d.close();d.addEventListener('close',()=>{anchor.replaceWith(svg);opener.focus()});
for(const id of ['boneSvg','chartSvg']){const el=document.getElementById(id),b=document.createElement('button');b.type='button';b.className='wolff-zoom';b.textContent='Ampliar';b.setAttribute('aria-label',id==='boneSvg'?'Ampliar estrutura óssea':'Ampliar gráfico de DMO');el.parentElement.append(b);b.onclick=()=>{svg=el;opener=b;anchor=document.createComment('posição do desenho');el.before(anchor);d.append(el);d.showModal()};}
})();
